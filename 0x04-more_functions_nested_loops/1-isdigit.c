#include "main.h"

/** 
 * 
 * include _isdigit 
 * 
 * Returns 1 if c is a digit
 * Returns 0 otherwise
 * 
 * Write a function that checks for a digit (0 through 9)
 */

int _isupper(int c)
{

if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);


}
 